// Re-export commonly used types
export type { Metadata } from 'next'

// TODO: Consolidate all inline type definitions from components and hooks into this centralized types file, including mock data structures
