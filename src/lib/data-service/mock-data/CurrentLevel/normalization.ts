import { mockCurrentUserLevels } from './mock'
import type { CurrentUserLevels, MovementCategory } from './types'// Main data export
export const currentLevelData = {
  currentLevels: mockCurrentUserLevels
}
